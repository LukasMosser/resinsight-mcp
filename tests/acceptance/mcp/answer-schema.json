{
  "type": "object",
  "properties": {
    "status": {"type": "string", "enum": ["observed", "no_image"]},
    "row": {"type": ["integer", "null"], "minimum": 1, "maximum": 4},
    "column": {"type": ["integer", "null"], "minimum": 1, "maximum": 4}
  },
  "required": ["status", "row", "column"],
  "additionalProperties": false
}
