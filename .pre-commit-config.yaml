repos:
  - repo: local
    hooks:
      - id: foundation
        name: Run repository checks
        entry: uv run --locked python scripts/check.py
        language: system
        pass_filenames: false
        always_run: true
