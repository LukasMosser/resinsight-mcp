# Installed OPM launcher checks

The full launcher composition passed 658 maintained tests at source `38e5aa8d9eca0c81e72ffdbdddfcf561e8735cbe`.
The [shared log](shared-check.log) records Ruff, ty, pytest, and strict MkDocs.
The [environment](environment.json) identifies exact tool versions, source review, and remaining acceptance gates.

## Clean installation

The [installation record](installation.json) identifies the clean source, application wheel, interpreter, native build, matching generated client, and exact commands.
The [dependency log](dependency-install.log) used locked cached packages in a new external environment.
The [build log](wheel-build.log) and [install log](wheel-install.log) record an ordinary application wheel and matching noneditable RIPS wheel.
The installed interpreter used Python 3.12.13 on macOS 14.2.1 arm64.
No package downloads or source-path overrides supplied application behavior.

The matching client was generated after the native prepared-grid export command.
Two later native fixes changed only internal grid source loading and property names.
They did not change the generated interfaces.
The installation record preserves the native source status and those source changes.

## Public discovery

The [recorded script](catalog-check-script.txt) uses the official SDK and installed interpreter with Python isolation enabled.
Its [launcher command](launcher-command.json) enables the complete OPM configuration with an explicit local Docker executable.
The [help output](launcher-help.log) records the shipped command options.
The launcher checked actual local parser, native client, Docker, and pinned image dependencies before creating an empty workspace.

The [tool list](tools.json) and [catalog resource](catalog.json) contain the same 46 tools.
The [acceptance record](acceptance.json) confirms discovery and the job, case restoration, and view discovery request shapes.
The [installed package record](installed-packages.json) identifies noneditable origins and versions separately from the source environment.
The SDK initialization, resource list, launcher output, and script output remain beside these records.

No domain tool was called during this check.
No native application or simulator was started.
The empty local workspace remains outside the repository and is omitted from this evidence bundle.

## Remaining acceptance

These checks establish installed launcher configuration, discovery, and schema agreement.
They do not establish completed P12 native loading or P13 simulation, comparison, cancellation, and project recovery.
The reviewed native trials and rendered documentation review remain pending owner approval.
