# Final P13 integration

The [accepted public trial](../public-workflow/README.md) establishes the generated FIELD workflow through the installed launcher on macOS.
The [source and evidence reviews](review.json) found no blocking issue in the driver, recorded payload, or delivery scope.
The final branch contains the P13 driver, documentation, and reviewed evidence above the merged domain and launcher changes.
It changes no production application code.

## Maintained checks

The [shared command log](shared-check.log) records 774 passing tests, Ruff, formatting, ty, and strict MkDocs.
The [check environment](shared-check-environment.json) identifies source `b2724755ab223990a7bb19f425ad3fa064c64ff8` and the evidence edits present during that check.
The final branch has identical application code, tests, dependency files, and check scripts.
The [integration environment](environment.json) records the exact source comparison commands, final documentation state, and tool versions.
The [final documentation log](documentation-check.log) verifies the integrated navigation, delivery pages, and evidence links.
Normal repository hooks run the complete shared command for the final integration commit.

## Accepted runtime source

The final application source matches installed source `b4c414beee91a0fa419f04e7c7874d2b2108a5a7`.
The final acceptance driver matches the executed source `b2724755ab223990a7bb19f425ad3fa064c64ff8`.
The [original runtime record](../public-workflow/trial-04/versions.json) preserves the ordinary application installation and matching generated RIPS client.
The [native build record](../../p12/native/README.md) preserves the required custom ResInsight build and its configuration.
Integration does not rerun or alter the accepted September 9 runtime records.

The [browser review](browser-review.json) records inspection of the final acceptance guide, delivery page, and implementation plan.
These documentation screenshots remain separate from the 29 original native acceptance images.

## Delivery boundary

The P13 evidence completes the bounded public workflow criteria for issues 14 and 35.
Wider recovery acceptance remains under P16, while distribution and release acceptance remain under P17.
Acceptance requires the recorded custom native build and does not establish compatibility with a released native binary.
The original evidence records retain their historical review and delivery status.
The pull request and required GitHub checks establish final integration status.

## Compact evidence review

The [raw evidence archive](../public-workflow/raw-evidence.tar.gz) keeps bulk generated records out of the text review.
Acceptance decisions, commands, completion tables, and all 29 accepted native images remain ordinary files.
The [packaging review](packaging-review.json) accounts for all 1,650 original files across 1,596 archive members and 54 retained paths.
Independent review compared parsed records, simulator arrays, source snapshots, project data, and decoded images with the expanded source payload.
The archive index and extraction instructions passed separate documentation review.
Application code, driver code, and accepted runtime records remain unchanged.

The [packaging check log](packaging-check.log) records 774 passing tests and the complete shared command.
Its [environment](packaging-check-environment.json) identifies the expanded source commit and uncommitted packaging state.
The earlier integration and browser records describe their original source states.
The [compact browser review](packaging-browser-review.json) records the rendered archive instructions and their [original screenshot](browser/archive-instructions.png).
The final commit also runs the normal repository hook against the compact package and integrated documentation.
