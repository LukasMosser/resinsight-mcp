# P06 observer evidence

Trial 04 ran on September 9, 2026.
The environment record identifies Python source `b0b41f65bb72599e6f12ba647cb4d47c2f53446c` and native source `1859d96e9a40093abfc43c42df9e9ef5b3846c9d`.
Raw records retain their original content, including local paths and test identifiers.
The original automated audit requires a separate visual review.
Its `visual_claims_verified: false` field does not report a failed visual review.

The package omits duplicate source snapshots, workspace databases, decks, and simulator output directories.
Those local files supported the trusted setup and remain outside this evidence package.
The recorded source commits identify the tested implementation.
The P01 evidence contains the original simulator run.

The `trial-03` directory preserves the failed predecessor audit, answer, events, and environment.
That trial captured no images because the client refused unapproved write tools.
The source repair configures approval only for `view_apply` and `view_render` in this isolated fixture.

The lead inspected the updated evidence page in an isolated Chrome browser.
The [browser record](browser-review/review.json) includes versions, image link checks, and screenshot names.
The separate [page review](browser-review/visual-review.json) records the visible layout findings.
This browser review does not complete the native editor inspection.
